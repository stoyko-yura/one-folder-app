{
  "name": "@one-folder-app/database",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "license": "ISC",
  "devDependencies": {
    "@one-folder-app/tsconfig": "workspace:*",
    "prisma": "^5.4.1"
  },
  "dependencies": {
    "@prisma/client": "^5.4.1"
  }
}
